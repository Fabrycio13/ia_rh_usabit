# Log de Atividades - 2026-05-05

## Alterações Realizadas

### Frontend / UI
- **Remoção de linha vertical no extremo direito**: Identificado que o `ChatWidget` possuía uma borda esquerda fixa (`borderLeft: '1px solid var(--border)'`), que ficava visível mesmo quando o painel estava fechado (largura 0px).
- **Correção**: A borda agora é condicional à propriedade `isOpen`.
- **Correção de erro de sintaxe em Pipeline.tsx**: Removida uma chave extra (`}`) que impedia a compilação.

## Arquivos Modificados
- `src/layouts/ChatWidget.tsx`
- `src/pages/candidates/Pipeline.tsx`

## Testes Realizados
- [x] Verificação de código: Confirmado que `borderLeft` agora depende de `isOpen`.
- [x] Verificação de lógica: Quando `isOpen` é falso, a largura é 0 e a borda é `none`, eliminando o artefato visual.
- [x] Correção de sintaxe: Removida chave extra em `Pipeline.tsx` e confirmado fim do erro de compilação.
