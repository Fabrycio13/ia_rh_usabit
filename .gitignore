# Logs
logs/*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Empty/legacy root files
errors.txt
schema.sql

# Frameworks de agents/skills não integrados ao projeto
.agent/
openspec/

# Test sprite MCP (config + logs ficam só locais — contém credenciais)
testsprite_tests/
.testsprite/

# Scratch files (one-off debug scripts e outputs temporários)
scratch/

# Project structure - Track these folders
!docs/
!scripts/
!logs/

# But ignore general logs inside logs folder if any
logs/*.log
# (Specific known logs like ts_errors.txt will be tracked because they are added)

testsprite_tests\tmp\config.json
